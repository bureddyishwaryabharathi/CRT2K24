#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,sum=1;
    for(;;){
    scanf("%d",&n);
        sum=sum+n;
        if(n==-1)
            break;
    }
    printf("%d",sum);
    

        
    return 0;
}
