#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int d,m,y,n;
    scanf("%d%d%d%d",&d,&m,&y,&n);
    int cinema=d+m+y;
     if((n>=50) && (n<=100)){
        if(cinema%(3*4)==0)
        {
            printf("Cinema Day");
        }
            
        
         else{
                printf("Not a Cinema Day");
            }
        }

      
    return 0;
}
